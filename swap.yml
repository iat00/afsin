---
- hosts: host03:host08:host22:host21
  remote_user: root
  tags: swap
  tasks:
    - shell: fallocate -l 1073741824 /swap01
        creates=/swap01
    - shell: mkswap /swap01
    - file: path=/swap01 mode=0600 owner=root group=root
    - mount: src=/swap01 name=none fstype=swap opts=sw passno=0 dump=0 state=present
    - shell: swapon /swap01
